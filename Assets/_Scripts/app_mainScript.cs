using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Vuforia;

public class app_mainScript : MonoBehaviour {
    public GameObject make;

	// Use this for initialization
	void Start () {
        //var foundMakes = FindObjectsOfType<ImageTarget>();
        //for (var i = 0; i<foundMakes.Size();i++)
            //script pe image target de logo, cand un logo este detectat, dam findall pe celelalte image target si le dam disable
            //(toate componentele ce tin de un model sunt parentate pe image target)
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
